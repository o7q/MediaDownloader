{
  "name": "media-downloader",
  "private": true,
  "version": "5.0.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev --config backend/tauri.conf.json",
    "build": "tauri build --no-bundle --config backend/tauri.conf.json"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/plugin-dialog": "^2.2.2"
  }
}
