{
  "name": "media-downloader",
  "private": true,
  "version": "5.0.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev --config src/backend/tauri.conf.json",
    "build": "tauri build --no-bundle --config src/backend/tauri.conf.json"
  },
  "dependencies": {
    "@tauri-apps/cli": "^2",
    "@tauri-apps/plugin-dialog": "^2"
  }
}
